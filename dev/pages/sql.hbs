---
{
	"title": "SQL Test",
	"template": "full-html",
	"firstname": "Caro",
	"tags": {
		"query": "SELECT * FROM users WHERE lastname LIKE :lastname;"
	},
	"new": {
		"query": "INSERT INTO users (firstname, lastname) VALUES (:firstname, :lastname);"
	}
}
---
<h1>{{ page.title }}</h1>

<hr>

<h2>All Persons</h2>
<ul>
{{#each page.tags}}
	<li>{{ this.id }}: {{ this.firstname }} {{ this.lastname }}</li>
{{/each}}
</ul>

<h2>First Person</h2>
<p>{{ page.tags._id }}: {{ page.tags._firstname }} {{ page.tags._lastname }}</p>

<hr>
<p>active lastname Filter: "{{ request.query.lastname }}"</p>